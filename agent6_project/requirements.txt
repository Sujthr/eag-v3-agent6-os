# Core
python-dotenv>=1.0
pydantic>=2.6
httpx>=0.27

# LLM Gateway (used from parent directory — no install needed)
# fastapi>=0.110
# uvicorn[standard]>=0.27

# MCP
mcp>=1.0

# UI
streamlit>=1.33

# MCP server dependencies (for the server script)
tavily-python>=0.3
duckduckgo-search>=6.0
crawl4ai>=0.3

# Tests
pytest>=8.0
pytest-asyncio>=0.23
